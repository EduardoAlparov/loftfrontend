.about-section{

}

.about {
  display: flex;
}

.about__user-data {
  flex: 1; 
  margin-right: 55px;
}

.about__content {
  flex: 2;
}

.about__content {
  display: flex;
  flex-direction: column;
}

.about__disclamer {
  font-size: 16px;
  line-height: 30px;
  color: #ffffff;
  font-weight: 600;
  opacity: 0.5;
  margin-bottom: 20px;
}

.about__title{
  font-size: 36px;
  line-height: 48px;
  color: #ffffff;
  font-weight: 700;
  margin-bottom: 30px;
}

.about__desc {
  font-size: 16px;
  line-height: 30px;
  color: #ffffff;
  font-weight: 600;
  opacity: 0.5;
  margin-bottom: 90px;
}

.about__ps {
  position: relative;
  font-size: 16px;
  line-height: 30px;
  color: #ffffff;
  font-weight: 400;
  font-style: italic;
  opacity: 0.5;
}

.about__icon {
  position: absolute;
  width: 23px;
  height: 23px;
  fill: #ff9a00;
  top: 5%;
  left: -8%;
}